import { Navbar as MantineNavbar } from '@mantine/core';
function Navbar() {
    return (
        <MantineNavbar width={{ base: 300 }} className="backdrop-blur-xl bg-[#FFFFFFCC]" p="xs">
            {/* Navbar content */}
        </MantineNavbar>
    );
}

export default Navbar;